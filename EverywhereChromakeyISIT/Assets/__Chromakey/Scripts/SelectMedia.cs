using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEditor;

public class SelectMedia : MonoBehaviour
{
   /* string path;
    public RawImage background;

    public void OpenExplorer()
    {
        path = EditorUtility.OpenFilePanel("Choose media", "", "");
        GetImage();
    }

    void GetImage()
    {
        if (path != null)
        {
            UpdateImage();
        }
    }

    void UpdateImage()
    {
        WWW www = new WWW("file:///" + path);
        background.texture = www.texture;
    }*/
}
